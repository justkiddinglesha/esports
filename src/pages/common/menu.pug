mixin menu(links)
    ul.menu
        each item in links
            li.menu__item
                a.menu__link(href=item.url)
                    i(class=item.iconClass)
                    span.menu__link-text=item.title